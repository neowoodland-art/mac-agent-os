# Matrix 本机数据目录配置
# 此文件由 install.sh 生成，每台机器独立，不参与坚果云同步
#
# local_data_root: 本机 agent-local/tools/matrix 目录的绝对路径

matrix:
  local_data_root: /Users/5kecheng/workbuddy-agent-os/agent-local/tools/matrix
